{
  // Worker name is deliberately brand-neutral (§0 brand isolation).
  "name": "k8-planner",
  "main": "src/index.ts",
  "compatibility_date": "2026-07-01",
  "vars": {
    "FIXTURE_MODE": "true"
  }
  // D1, KV, Queues, R2 (transient upload buffer w/ TTL), and Durable Objects
  // bindings land with their owning phases (1, 2, 3, 5) — each must be
  // justified in that phase's handoff per §2.
}
