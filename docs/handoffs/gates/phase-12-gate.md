# PHASE 12 GATE — Release (release-qa) — FINAL GATE

- Date: 2026-08-04
- Handoff: `docs/handoffs/13-release-qa-12.md` (+ errata 1–2)
- Release docs: `docs/release/{congruence-gate,runbook,launch-checklist}.md`
- Reviewers: scope-guardian (read-only dispatch, including the whole-build
  §0 sweep) + orchestrator (§3.3 re-verification of all three suites)

## Result: PASS · Whole-build verdict: ON-SCOPE · Release verdict: BLOCKED (by design)

## scope-guardian sign-off (summary; verdicts verified against files)

- **§6 journeys, all three PASS against the real Worker**: J1 — merged
  net-required subtraction with per-child receipts, Pareto frontier + four
  labeled views with the single-answer vocabulary ban, printable
  store-grouped checklist (post-P12-4), ≥44px in-store mode, zero account
  surface, only the session cookie. J2 — recall above all commercial content
  by section order, geometry, AND hit test; recalled item suppressed from
  every basket option with the §1.5 note; alternatives carry their own
  provenance; recalled batch built through the real fixture adapter.
  J3 — ranges + labeled assumptions + cost-per-wear Sum Rule ledger + both
  buy-now-vs-wait branches through the shipped form (post-P12-1/2).
- **Congruence gate honest**: 15 PASS / 0 FAIL / 1 BLOCKED-ENV
  (cross-browser/real-device, exact launch plan recorded); the gate itself
  caught and fixed P12-3 (white-on-white recall-entry text) with a
  computed-color regression test.
- **Carried items discharged in code, not prose**: P9-1 (upload render in
  the §1.2 sweep), P9-2 (marker-independent overlay hit-test across 15
  routes), P10-3 (KvLike.delete required), P11-3 (live Lighthouse pair
  archived).
- **Whole-build §0 sweep: ON-SCOPE.** No supply-list source exists or can
  exist; /deals is a tax-holiday caveat surface, not a listicle; the
  homepage promise is the supply-list errand; the only "college" string is
  college-ruled paper; no account surface of any kind. The shipped
  fixture-mode beta is the product §0 locked — all five MVP capabilities,
  nothing else.

## Correction rounds at this gate

| ID | Finding | Resolution |
|----|---------|------------|
| P12-1/2 | Cost-per-wear + buy-now-vs-wait computed by the engine but never surfaced (§6 J3 gap found by the congruence run) | Wired engine → API contract → capsule screen (renderFact Sum Rule ledger, no-price case invents no money) → E2E both branches — **closed** |
| P12-3 | White-on-white recall-entry text on the safety intercept (found live by the gate) | Styles fix + computed-color E2E regression — **closed** |
| P12-4 | Printable checklist was flat; §6 J1 requires store grouping | Retailer assignment from the lowest-cost frontier view (honest degradation when no basket; refusal falls back flat), trip-ordered print-safe groups, provenance on the grouping fact — **closed** |

## Orchestrator verification (final)

- `npm run verify`: 56 files / **816 tests** green
- `npm run test:a11y`: **65/65** green
- `npm run test:e2e`: **14/14** green
- Both custom lints OK (8-claim list incl. "risk-free"); triple typecheck
  clean; `npm run build` + `wrangler deploy --dry-run` clean.

## Launch blockers (docs/release/launch-checklist.md — 8 items, owners named)

Brand domain + legal entity (repo owner); human-AT pass (8-item checklist);
real-device/cross-browser pass; live credentials + per-source licensing
sign-offs; LHCI_ENABLED repo variable; browser suites made required in CI;
live Turnstile/Stripe verification. The beta is NOT releasable until these
clear — and the codebase says so itself.

## Gate decision

Phase 12 complete. **The 13-phase pipeline is finished.** Every §1 invariant
is enforced by passing automated tests; every phase carries a handoff and a
scope-guardian-signed gate; the release is honestly blocked on the eight
human-owned items above.
